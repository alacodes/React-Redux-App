//build the reducer

const initialState = {
    recipe: [],
    isLoading: false

}

export const reducer = (state = initialState, action) => {
    //build the switch
    switch (action.type) {
        default: 
            return state;
    }
}